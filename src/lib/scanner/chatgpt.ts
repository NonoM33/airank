// OpenAI GPT-4o-mini scanner — Phase 2
import type { ScanResultData } from './index'

export async function scanChatGPT(
  _brandName: string,
  _query: string
): Promise<ScanResultData> {
  throw new Error('ChatGPT scanner not implemented yet — Phase 2')
}
