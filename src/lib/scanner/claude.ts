// Anthropic Claude Haiku scanner — Phase 2
import type { ScanResultData } from './index'

export async function scanClaude(
  _brandName: string,
  _query: string
): Promise<ScanResultData> {
  throw new Error('Claude scanner not implemented yet — Phase 2')
}
