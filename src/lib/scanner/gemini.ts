// Google Gemini Flash scanner — Phase 2
import type { ScanResultData } from './index'

export async function scanGemini(
  _brandName: string,
  _query: string
): Promise<ScanResultData> {
  throw new Error('Gemini scanner not implemented yet — Phase 2')
}
